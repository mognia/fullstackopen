sequenceDiagram
participant browser
participant server

    Note right of browser: User writes a new note in the input field

    Note right of browser: Save button is clicked

    Note right of browser: JavaScript prevents default form submission

    browser->>server: POST /exampleapp/new_note_spa (JSON data)
    activate server
    server-->>browser: Response confirming note was saved
    deactivate server

    Note right of browser: JavaScript updates the notes list in the UI
